/**
 * @flow
 * @prettier
 */

import * as React from 'react'

import Typography from '@material-ui/core/Typography'

export type Props = {}

const UpgradesTitle = (props: Props): React.Node => (
  <Typography variant="h3">Apply Upgrades</Typography>
)

export default UpgradesTitle
